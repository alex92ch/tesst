apiVersion: resources.teleport.dev/v2
kind: TeleportProvisionToken
metadata:
  name: bootstrap
  namespace: teleport-cluster
spec:
  roles: [Bot]
  join_method: kubernetes
  bot_name: bootstrap
  kubernetes:
    type: in_cluster
    allow:
    - service_account: "teleport-cluster:teleport-bootstrap"